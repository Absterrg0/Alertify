import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['utfs.io','075uvr3bfp.ufs.sh'], // Add 'utfs.io' here to allow images from this domain
  },
};

export default nextConfig;
